package jetbrains.mps.vclang.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class ElimCaseSwitch_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<ElimCaseSwitch> constants = ListSequence.fromList(ElimCaseSwitch.getConstants()).iterator();
    while (constants.hasNext()) {
      ElimCaseSwitch constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<ElimCaseSwitch> constants = ListSequence.fromList(ElimCaseSwitch.getConstants()).iterator();
    while (constants.hasNext()) {
      ElimCaseSwitch constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    ElimCaseSwitch constant = ElimCaseSwitch.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
